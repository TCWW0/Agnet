# Agent Chat Front (deepresearch/front)

这是一个最小化的前端骨架，用于 Agent 项目，初始放在 `deepresearch/front` 目录，用于 Phase 1 开发与迭代。

快速开始（在项目根 `deepresearch/front` 中运行）:

```bash
npm install
npm run dev
```

说明:

- 该目录包含基础的 React + TypeScript 文件结构和一个简单的消息 mock 演示；
- 按研究文档建议，可在后续步骤中添加 Tailwind、流式集成和测试配置。
